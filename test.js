// function solution(numbers) {
//   return (numbers => numbers.reduce((p, c) => p + c, 0) / numbers.length);
//}
let number = [1,2,3,4,5.5]
const a = (number) => number.reduce((a,b) => a+b, 0) / number.length
console.log(a)


